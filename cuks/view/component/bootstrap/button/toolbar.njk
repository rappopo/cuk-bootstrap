<div class="btn-toolbar{% if params.stacked %}-vertical{% endif %}"
  role="toolbar" {% if params.ariaLabel %}aria-label="{{ params.ariaLabel }}"{% endif %}
  {% if params.id %}id="{{ params.id }}"{% endif %}
  >
  {% for v in params.groups %}
    {{ v }}
  {% endfor %}
</div>
